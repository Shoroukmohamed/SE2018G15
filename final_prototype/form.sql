


CREATE TABLE `mom` (
  `name` text NOT NULL,
  `password` int(55) NOT NULL,
  `email` text NOT NULL,
  `mom` text NOT NULL,
  `age` int(2) NOT NULL,
  `which` int(2) NOT NULL,
   `Number` int(2) NOT NULL);